import { Injectable } from '@angular/core';
//import { cyptojs } from 'crypto-js'; 


@Injectable({
  providedIn: 'root'
})
export class EncriptionService {

  // private key = enc.Utf8.parse(1203199320052021);
  //   private iv = enc.Utf8.parse(1203199320052021);
  //   constructor() {}
  //   // Methods for the encrypt and decrypt Using AES
  //   encryptUsingAES256(text): any {
  //       var encrypted = AES.encrypt(enc.Utf8.parse(text), this.key, {
  //           keySize: 128 / 8,
  //           iv: this.iv,
  //           mode: mode.CBC,
  //           padding: pad.Pkcs7
  //       });
  //       return encrypted.toString();
  //   }
  //   decryptUsingAES256(decString) {
  //       var decrypted = AES.decrypt(decString, this.key, {
  //           keySize: 128 / 8,
  //           iv: this.iv,
  //           mode: mode.CBC,
  //           padding: pad.Pkcs7
  //       });
  //       return decrypted.toString(enc.Utf8);
  //   }
  }
