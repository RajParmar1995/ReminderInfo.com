import { MonthtoyearPipe } from './monthtoyear.pipe';

describe('MonthtoyearPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthtoyearPipe();
    expect(pipe).toBeTruthy();
  });
});
