import { SelectfilterPipe } from './selectfilter.pipe';

describe('SelectfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
